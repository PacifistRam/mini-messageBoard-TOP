extends layout

block content
  h1= title
  p Welcome to #{title} 
  
  a(href = '/new') write new message
  div
    p you have received #{messageArray.length} messages
      each message in messageArray 
        p 
          b #{message.user} : #{message.text}  
        | message sent at #{message.added}
        br